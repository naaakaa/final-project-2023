'use client';

import style from './SignUpButton.module.scss';

export default function SignUpButton() {
  return (
    <button className={style.signUpButton} onClick={() => {}}>
      Sign up
    </button>
  );
}
